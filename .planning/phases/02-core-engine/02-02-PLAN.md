---
phase: 02-core-engine
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/tileBag.ts
  - src/lib/__tests__/tileBag.test.ts
autonomous: true
requirements:
  - TILE-01
  - TILE-02
  - TILE-03
  - TILE-04
  - TILE-05

must_haves:
  truths:
    - "dealHand(bag, 9) returns exactly 9 tiles and removes them from the bag"
    - "drawToNine tops up a hand to 9 tiles from the bag"
    - "createBag('bananagrams') produces 144 tiles with correct per-letter counts"
    - "createBag('scrabble') produces 98 tiles with correct per-letter counts"
    - "Dealing 9 tiles from Bananagrams bag produces at least 2 vowels (statistical, 100 trials)"
    - "Q tile is stored as single character 'Q' in the bag, never 'QU'"
    - "drawToNine does not exceed 9 even if bag has more tiles"
    - "drawToNine handles empty bag gracefully"
  artifacts:
    - path: "src/lib/tileBag.ts"
      provides: "Tile bag creation, dealing, and draw-to-nine functions"
      exports: ["createBag", "dealHand", "drawToNine", "BANANAGRAMS_COUNTS", "SCRABBLE_COUNTS", "TileDistribution"]
    - path: "src/lib/__tests__/tileBag.test.ts"
      provides: "Unit and statistical tests for tile bag"
      min_lines: 60
  key_links:
    - from: "src/lib/tileBag.ts"
      to: "BANANAGRAMS_COUNTS / SCRABBLE_COUNTS"
      via: "internal constants"
      pattern: "BANANAGRAMS_COUNTS|SCRABBLE_COUNTS"
---

<objective>
TileBag module: Bananagrams and Scrabble tile distributions, Fisher-Yates shuffle, deal hand, draw-to-nine refill.

Purpose: All tile management as pure functions, fully unit-tested via TDD. The tile bag is the source of randomness for the game.
Output: src/lib/tileBag.ts (implementation), src/lib/__tests__/tileBag.test.ts (tests)
</objective>

<execution_context>
@C:/Users/travi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/travi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-engine/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing TileBag tests</name>
  <files>src/lib/__tests__/tileBag.test.ts</files>
  <behavior>
    - createBag('bananagrams') returns array of length 144
    - createBag('scrabble') returns array of length 98
    - createBag('bananagrams') contains exactly 13 A's, 18 E's, 2 Q's (spot-check)
    - createBag('scrabble') contains exactly 9 A's, 12 E's, 1 Q (spot-check)
    - Every tile in bag is a single uppercase letter A-Z (Q is 'Q', not 'QU')
    - dealHand(bag, 9) returns array of length 9
    - dealHand mutates bag: bag.length decreases by 9
    - dealHand returns only valid uppercase letters
    - drawToNine(['A','B','C'], bag) returns array of length 9
    - drawToNine with hand already at 9 returns same hand, bag unchanged
    - drawToNine with empty bag returns hand as-is
    - Statistical: 100 trials of dealHand(createBag('bananagrams'), 9) all produce >= 2 vowels
  </behavior>
  <action>
    Create `src/lib/__tests__/tileBag.test.ts` with all tests listed above. Import from `../tileBag` (file does not exist yet). Group into describe blocks: "createBag", "dealHand", "drawToNine", "statistical properties".

    For the statistical vowel test, use `const VOWELS = new Set(['A','E','I','O','U'])` and loop 100 trials, asserting zero failures.

    Do NOT test exact tile ordering — only test counts, membership, and statistical properties (research Pitfall 6).

    RED phase: run `npm test -- --run src/lib/__tests__/tileBag.test.ts` and confirm all tests fail.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/__tests__/tileBag.test.ts 2>&1 | grep -E "FAIL|fail|error" | head -5</automated>
  </verify>
  <done>tileBag.test.ts has 12+ test cases, all failing (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TileBag to pass all tests</name>
  <files>src/lib/tileBag.ts</files>
  <behavior>
    All tests from Task 1 pass GREEN.
  </behavior>
  <action>
    Create `src/lib/tileBag.ts` implementing:

    1. **Exported constants**:
       - `BANANAGRAMS_COUNTS: Record<string, number>` — A:13, B:3, C:3, D:6, E:18, F:3, G:4, H:3, I:12, J:2, K:2, L:5, M:3, N:8, O:11, P:3, Q:2, R:9, S:6, T:9, U:6, V:3, W:3, X:2, Y:3, Z:2 (144 total)
       - `SCRABBLE_COUNTS: Record<string, number>` — A:9, B:2, C:2, D:4, E:12, F:2, G:3, H:2, I:9, J:1, K:1, L:4, M:2, N:6, O:8, P:2, Q:1, R:6, S:4, T:6, U:4, V:2, W:2, X:1, Y:2, Z:1 (98 total)

    2. **Exported type**: `TileDistribution = 'bananagrams' | 'scrabble'`

    3. **Internal helper**:
       - `fisherYatesShuffle(arr: string[]): string[]` — canonical Fisher-Yates: loop from `arr.length - 1` down to `1`, swap `arr[i]` with `arr[random 0..i]`. Mutates and returns the array. Use `Math.floor(Math.random() * (i + 1))` for the random index (research Pitfall 3).

    4. **Exported functions**:
       - `createBag(distribution: TileDistribution): string[]` — builds array from counts, Fisher-Yates shuffles, returns. Each tile is a single uppercase letter. Q is stored as `'Q'`, never `'QU'`.
       - `dealHand(bag: string[], count: number): string[]` — `bag.splice(0, count)`. Mutates the bag. Returns dealt tiles.
       - `drawToNine(hand: string[], bag: string[]): string[]` — computes `needed = 9 - hand.length`. If needed <= 0 or bag empty, returns hand as-is. Otherwise returns `[...hand, ...dealHand(bag, Math.min(needed, bag.length))]`.

    GREEN phase: run `npm test -- --run src/lib/__tests__/tileBag.test.ts` and confirm all tests pass.
    Then run `npm test -- --run` to confirm no regressions.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/__tests__/tileBag.test.ts</automated>
  </verify>
  <done>All tileBag tests pass GREEN. All existing tests still pass. createBag, dealHand, drawToNine exported and working.</done>
</task>

</tasks>

<verification>
npm test -- --run
All tests pass including tileBag tests and all pre-existing tests.
</verification>

<success_criteria>
- createBag('bananagrams') returns 144 tiles with correct letter counts
- createBag('scrabble') returns 98 tiles with correct letter counts
- dealHand removes and returns correct number of tiles
- drawToNine refills hand to 9 from bag
- Q tile stored as 'Q' (single char) in all arrays
- Statistical: 9-tile hands consistently have >= 2 vowels from Bananagrams distribution
- All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-engine/02-02-SUMMARY.md`
</output>
