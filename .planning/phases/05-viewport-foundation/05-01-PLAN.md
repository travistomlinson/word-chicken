---
phase: 05-viewport-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/screens/ConfigScreen.tsx
  - src/screens/LobbyScreen.tsx
  - src/__tests__/App.test.tsx
autonomous: true
requirements:
  - VPRT-01
  - VPRT-03

must_haves:
  truths:
    - "App loading state uses min-h-svh (conservative viewport), not min-h-screen"
    - "App error state uses min-h-svh, not min-h-screen"
    - "App main wrapper uses min-h-dvh (dynamic viewport), not min-h-screen"
    - "ConfigScreen outer container uses min-h-dvh, not min-h-screen"
    - "LobbyScreen outer container uses min-h-dvh, not min-h-screen"
  artifacts:
    - path: "src/App.tsx"
      provides: "Viewport-correct shell wrappers"
      contains: "min-h-svh"
    - path: "src/App.tsx"
      provides: "Viewport-correct main wrapper"
      contains: "min-h-dvh"
    - path: "src/screens/ConfigScreen.tsx"
      provides: "Scrollable full-viewport config screen"
      contains: "min-h-dvh"
    - path: "src/screens/LobbyScreen.tsx"
      provides: "Scrollable full-viewport lobby screen"
      contains: "min-h-dvh"
    - path: "src/__tests__/App.test.tsx"
      provides: "Viewport class assertions for App states"
  key_links:
    - from: "src/App.tsx"
      to: "CSS min-h-svh / min-h-dvh"
      via: "Tailwind class on wrapper divs"
      pattern: "min-h-svh|min-h-dvh"
---

<objective>
Replace min-h-screen with viewport-correct CSS units on App shell and scrollable screens.

Purpose: Fix VPRT-01 (app fills visible viewport on first load) and VPRT-03 (Config/Lobby scroll gracefully) by swapping 100vh-based min-h-screen to dvh/svh units that respect mobile browser chrome.
Output: App.tsx uses min-h-svh on loading/error, min-h-dvh on main wrapper. ConfigScreen and LobbyScreen use min-h-dvh. Tests verify class presence.
</objective>

<execution_context>
@C:/Users/travi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/travi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-viewport-foundation/05-RESEARCH.md

<interfaces>
<!-- Current class patterns to replace -->

From src/App.tsx:
- Line 73: `bg-surface min-h-screen font-jost flex items-center justify-center` (loading state)
- Line 81: `bg-surface min-h-screen font-jost flex items-center justify-center` (error state)
- Line 88: `bg-surface min-h-screen font-jost` (main wrapper)

From src/screens/ConfigScreen.tsx:
- Line 94: `min-h-screen bg-gradient-to-br ...` (outer div)

From src/screens/LobbyScreen.tsx:
- Line 146: `min-h-screen bg-gradient-to-br ...` (outer div)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add viewport class assertions to App.test.tsx</name>
  <files>src/__tests__/App.test.tsx</files>
  <behavior>
    - Loading state container has class min-h-svh and does NOT have class min-h-screen
    - Error state container has class min-h-svh and does NOT have class min-h-screen
    - Main app wrapper (when dictionary ready) has class min-h-dvh and does NOT have class min-h-screen
  </behavior>
  <action>
Extend the existing App.test.tsx with three new test cases in the existing describe block:

1. "loading state uses min-h-svh viewport unit" — set dictionary status to 'loading', render App, find the container div (parent of "Loading dictionary..." text), assert it has class `min-h-svh` and does NOT have class `min-h-screen`.

2. "error state uses min-h-svh viewport unit" — set dictionary status to 'error', render App, find the container div (parent of "Failed to load dictionary." text), assert it has class `min-h-svh` and does NOT have class `min-h-screen`.

3. "main wrapper uses min-h-dvh viewport unit" — dictionary status 'ready', render App, find the outermost div wrapping the ConfigScreen (use container query or closest parent with min-h class), assert it has class `min-h-dvh` and does NOT have class `min-h-screen`.

Use `closest()` or `.parentElement` to get the wrapper div from the text element. These tests MUST fail initially (red) because App.tsx currently uses min-h-screen.
  </action>
  <verify>
    <automated>cd C:/Users/travi/Claude_Projects/Word_Chicken && npm test -- --run src/__tests__/App.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Three new viewport tests exist in App.test.tsx. They fail because App.tsx still uses min-h-screen.</done>
</task>

<task type="auto">
  <name>Task 2: Replace min-h-screen with dvh/svh on App, Config, and Lobby</name>
  <files>src/App.tsx, src/screens/ConfigScreen.tsx, src/screens/LobbyScreen.tsx</files>
  <action>
Make these exact class replacements (search and replace within className strings):

**src/App.tsx:**
- Line 73 (loading div): replace `min-h-screen` with `min-h-svh`
- Line 81 (error div): replace `min-h-screen` with `min-h-svh`
- Line 88 (main wrapper div): replace `min-h-screen` with `min-h-dvh`

**src/screens/ConfigScreen.tsx:**
- Line 94 (outer div): replace `min-h-screen` with `min-h-dvh`

**src/screens/LobbyScreen.tsx:**
- Line 146 (outer div): replace `min-h-screen` with `min-h-dvh`

No other changes. Do not add overscroll-none to these screens (that belongs on GameScreen only). Do not add env(safe-area-inset-*) — that is Phase 6. Do not change any other classes on these elements.

After changes, verify zero occurrences of `min-h-screen` remain in these three files.
  </action>
  <verify>
    <automated>cd C:/Users/travi/Claude_Projects/Word_Chicken && npm test -- --run src/__tests__/App.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All three App.tsx viewport tests pass (green). ConfigScreen and LobbyScreen use min-h-dvh. No min-h-screen remains in App.tsx, ConfigScreen.tsx, or LobbyScreen.tsx.</done>
</task>

</tasks>

<verification>
- `grep -r "min-h-screen" src/App.tsx src/screens/ConfigScreen.tsx src/screens/LobbyScreen.tsx` returns no matches
- `grep "min-h-svh" src/App.tsx` returns 2 matches (loading + error states)
- `grep "min-h-dvh" src/App.tsx` returns 1 match (main wrapper)
- `grep "min-h-dvh" src/screens/ConfigScreen.tsx` returns 1 match
- `grep "min-h-dvh" src/screens/LobbyScreen.tsx` returns 1 match
- `npm test -- --run src/__tests__/App.test.tsx` all pass
</verification>

<success_criteria>
- App loading/error states render with min-h-svh (conservative viewport height)
- App main wrapper renders with min-h-dvh (dynamic viewport height)
- ConfigScreen and LobbyScreen outer containers render with min-h-dvh
- All existing tests continue to pass
- Three new viewport-specific tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-viewport-foundation/05-01-SUMMARY.md`
</output>
