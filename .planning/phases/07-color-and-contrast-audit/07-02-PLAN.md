---
phase: 07-color-and-contrast-audit
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/screens/GameScreen.tsx
  - src/screens/LobbyScreen.tsx
  - src/components/ScorePanel.tsx
  - src/components/TurnIndicator.tsx
  - src/components/RoundEndCard.tsx
  - src/components/GameOverScreen.tsx
  - src/components/SharedWordDisplay.tsx
  - src/components/StagingArea.tsx
  - src/components/WordHistory.tsx
  - src/components/PlayerHand.tsx
  - src/__tests__/color-contrast.test.ts
autonomous: true
requirements: [COLR-01, COLR-02]

must_haves:
  truths:
    - "Every informational text element passes 4.5:1 contrast in light mode"
    - "Every informational text element passes 4.5:1 contrast in dark mode"
    - "No informational text uses text-ink with opacity below /70"
    - "Brand color text (blue/red) uses accent tokens that auto-switch in dark mode"
  artifacts:
    - path: "src/screens/GameScreen.tsx"
      provides: "Contrast-safe text classes for round counter, quit button, status messages"
    - path: "src/components/ScorePanel.tsx"
      provides: "Accent tokens for You/AI labels instead of raw brand colors"
      contains: "text-accent-primary"
    - path: "src/components/TurnIndicator.tsx"
      provides: "Accent tokens for turn phase labels"
      contains: "text-accent-danger"
    - path: "src/components/RoundEndCard.tsx"
      provides: "ink-secondary for all secondary labels"
      contains: "text-ink-secondary"
    - path: "src/components/GameOverScreen.tsx"
      provides: "ink-secondary for secondary labels, accent tokens for result colors"
      contains: "text-ink-secondary"
  key_links:
    - from: "src/components/ScorePanel.tsx"
      to: "src/index.css"
      via: "text-accent-primary and text-accent-danger reference tokens from Plan 01"
      pattern: "text-accent-(primary|danger)"
    - from: "src/components/TurnIndicator.tsx"
      to: "src/index.css"
      via: "text-accent-primary and text-accent-danger reference tokens from Plan 01"
      pattern: "text-accent-(primary|danger)"
---

<objective>
Replace all failing text-ink/N opacity classes with ink-secondary tokens and all dark-mode-failing brand color text with accent tokens across all game components.

Purpose: Achieves WCAG AA contrast compliance for all informational text in both light and dark mode, completing COLR-01 and COLR-02.
Output: All component files updated with contrast-safe text classes, COLR-01/02 tests enabled and passing.
</objective>

<execution_context>
@C:/Users/travi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/travi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-color-and-contrast-audit/07-RESEARCH.md
@.planning/phases/07-color-and-contrast-audit/07-01-SUMMARY.md

<interfaces>
<!-- New tokens created by Plan 01 in index.css -->
--color-ink-secondary: #6b6967;   /* light: ~4.6:1 on surface/card */
--color-accent-primary: #003f91;  /* light: blue, passes on light bg */
--color-accent-danger: #d0021b;   /* light: red, passes on light bg */

/* Dark mode overrides */
--color-ink-secondary: #a09e9a;   /* dark: ~4.5:1 on dark surfaces */
--color-accent-primary: #5b8de0;  /* dark: lighter blue, passes dark bg */
--color-accent-danger: #f07070;   /* dark: lighter red, passes dark bg */

<!-- Tailwind classes available after Plan 01 -->
text-ink-secondary  — secondary de-emphasized text (replaces text-ink/50, text-ink/40, text-ink/30)
text-accent-primary — blue-ish text that auto-switches to lighter blue in dark mode
text-accent-danger  — red-ish text that auto-switches to lighter red in dark mode
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace text-ink/N opacity classes in all components</name>
  <files>src/screens/GameScreen.tsx, src/screens/LobbyScreen.tsx, src/components/RoundEndCard.tsx, src/components/GameOverScreen.tsx, src/components/SharedWordDisplay.tsx, src/components/StagingArea.tsx, src/components/WordHistory.tsx, src/components/PlayerHand.tsx</files>
  <action>
Replace every failing `text-ink/N` usage with `text-ink-secondary` EXCEPT decorative "vs" and "or" separators (which are WCAG-exempt).

**Exact replacements per file:**

**GameScreen.tsx:**
- Line ~178: `text-ink/30` on round counter "R{n}" -> `text-ink-secondary`
- Line ~184: `text-ink/40` on Quit button -> `text-ink-secondary`
- Line ~68: `text-ink/50` on "Waiting for opponent..." -> `text-ink-secondary`
- Line ~130: `text-ink/60` on disconnect error message -> `text-ink-secondary`
- Line ~(vs separator if any): `text-ink/20` on "vs" -> LEAVE AS-IS (decorative)

**LobbyScreen.tsx:**
- Line ~154: `text-ink/50` on status message -> `text-ink-secondary`
- Line ~174: `text-ink/30` on "or" separator -> LEAVE AS-IS (decorative)
- Line ~185: `placeholder:text-ink/20` on input placeholder -> change to `placeholder:text-ink-secondary`
- Line ~201: `text-ink/50` on status label -> `text-ink-secondary`
- Line ~211: `text-ink/50` on "Waiting for opponent..." -> `text-ink-secondary`
- Line ~231: `text-ink/50` on "Connecting to lobby..." -> `text-ink-secondary`
- Line ~241: `text-ink/50` on "Connected! Starting game..." -> `text-ink-secondary`

**RoundEndCard.tsx:**
- Line ~57: `text-ink/50` on "Round {n}" label -> `text-ink-secondary`
- Line ~69: `text-ink/40` on "Word Chain" label -> `text-ink-secondary`
- Line ~75: `text-ink/50` on word chain entry player label -> `text-ink-secondary`
- Line ~79: `text-ink/50` on word chain score -> `text-ink-secondary`
- Line ~84: `text-ink/40` on longest word text -> `text-ink-secondary`
- Line ~94: `text-ink/50` on "Round Points" label -> `text-ink-secondary`
- Line ~97, ~101: `text-ink/40` on "You"/"AI" labels -> `text-ink-secondary`
- Line ~107: `text-ink/50` on "Total" label -> `text-ink-secondary`
- Line ~110, ~114: `text-ink/40` on "You"/"AI" labels -> `text-ink-secondary`

**GameOverScreen.tsx:**
- Line ~64: `text-ink/50` on "You" label -> `text-ink-secondary`
- Line ~69: `text-ink/20` on "vs" separator -> LEAVE AS-IS (decorative)
- Line ~71: `text-ink/50` on opponent label -> `text-ink-secondary`
- Line ~79: `text-ink/50` on "Played N rounds" -> `text-ink-secondary`
- Line ~85: `text-ink/50` on additional info text -> `text-ink-secondary`

**SharedWordDisplay.tsx:**
- Line ~17: `text-ink/40` on "Waiting for starting word..." placeholder -> `text-ink-secondary`

**StagingArea.tsx:**
- Line ~33: `text-ink/30` on "Tap tiles below" placeholder -> `text-ink-secondary`

**WordHistory.tsx:**
- Line ~21: `text-ink/30` on "History" label -> `text-ink-secondary`
- Line ~39: `text-ink/40` on score text -> `text-ink-secondary`

**PlayerHand.tsx:**
- Line ~386: `text-ink/40` on Give Up button -> `text-ink-secondary`

**Also update any `border-ink/10` or `border-ink/30` in the above files? NO.** Border and background opacity usages are not covered by COLR-01/02 (WCAG 1.4.3 is about text). Leave all `border-ink/N`, `bg-ink/N`, and `shadow-*` classes unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/color-contrast.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>No informational text uses text-ink with opacity below /70. All secondary text uses text-ink-secondary token. Decorative separators ("vs", "or") retain their low-opacity styling (WCAG-exempt).</done>
</task>

<task type="auto">
  <name>Task 2: Replace brand color text with accent tokens and enable COLR-01/02 tests</name>
  <files>src/components/ScorePanel.tsx, src/components/TurnIndicator.tsx, src/components/RoundEndCard.tsx, src/components/GameOverScreen.tsx, src/components/WordHistory.tsx, src/screens/LobbyScreen.tsx, src/screens/GameScreen.tsx, src/__tests__/color-contrast.test.ts</files>
  <action>
Replace `text-corbusier-blue` and `text-corbusier-red` used as TEXT colors (not bg- or shadow- prefixes) with `text-accent-primary` and `text-accent-danger` respectively. These new tokens auto-switch to lighter shades in dark mode via the CSS tokens created in Plan 01.

**Exact replacements:**

**ScorePanel.tsx:**
- Line ~26: `text-corbusier-blue` on "You" label -> `text-accent-primary`
- Line ~39: `text-corbusier-red` on opponent label -> `text-accent-danger`
- Also update the `bg-corbusier-blue/10` and `bg-corbusier-red/10` highlight backgrounds? These are subtle bg tints, not text — they may look wrong in dark mode with the original dark blue, but they are not COLR requirements. Leave them for Phase 8 visual polish.

**TurnIndicator.tsx:**
- Line ~22: `text-corbusier-red` on "Opponent choosing..." -> `text-accent-danger`
- Line ~37: `text-corbusier-blue` on "Your Turn" -> `text-accent-primary`
- Line ~47: `text-corbusier-red` on "AI is thinking..." / "Opponent is thinking..." -> `text-accent-danger`
- Also update `bg-corbusier-red` and `bg-corbusier-blue` on the pulse dots? These are small decorative indicators (2x2px dots), not text. Leave them — they are WCAG-exempt as decorative.

**RoundEndCard.tsx:**
- Line ~32: `winnerColor` variable uses `text-corbusier-blue` / `text-corbusier-red` -> change to `text-accent-primary` / `text-accent-danger`

**GameOverScreen.tsx:**
- Line ~25: `resultColor` variable uses `text-corbusier-blue` / `text-corbusier-red` -> change to `text-accent-primary` / `text-accent-danger`
- Line ~65: conditional `text-corbusier-blue` on winner score -> `text-accent-primary`
- Line ~72: conditional `text-corbusier-red` on loser score -> `text-accent-danger`

**WordHistory.tsx:**
- Line ~25: `labelColor` uses `text-corbusier-blue` / `text-corbusier-red` -> `text-accent-primary` / `text-accent-danger`

**LobbyScreen.tsx:**
- Line ~219: `text-corbusier-blue` on Copy Code button -> `text-accent-primary`
- Line ~219: `hover:text-corbusier-blue/70` -> `hover:text-accent-primary/70`
- Line ~219: `border-corbusier-blue/30` -> `border-accent-primary/30`
- Line ~249: `text-corbusier-red` on error message -> `text-accent-danger`

**GameScreen.tsx:**
- Line ~184: `hover:text-corbusier-red` on Quit button hover -> `hover:text-accent-danger`

**PlayerHand.tsx:**
- Line ~379: `text-corbusier-blue` on "Show a Word" button -> `text-accent-primary`
- Line ~379: `hover:text-corbusier-blue/70` -> `hover:text-accent-primary/70`
- Line ~379: `border-corbusier-blue/30` -> `border-accent-primary/30`
- Line ~386: `hover:text-corbusier-red` on Give Up hover -> `hover:text-accent-danger`

**DO NOT change:**
- `bg-corbusier-blue`, `bg-corbusier-red`, `bg-corbusier-yellow` (background colors — text on these is white/charcoal, already handled)
- `shadow-corbusier-*` (shadow colors — decorative)
- `text-corbusier-blue` in ConfigScreen.tsx line ~119 (the "How to Play" link text) -> DO change this to `text-accent-primary` and its hover `hover:text-corbusier-blue/70` to `hover:text-accent-primary/70`
- `text-corbusier-red` in App.tsx line ~82 (error text on load failure) -> change to `text-accent-danger`
- `text-corbusier-red` in StagingArea.tsx line ~50 (validation error) -> change to `text-accent-danger`

**Enable COLR-01/02 tests:**
In `src/__tests__/color-contrast.test.ts`, remove `describe.skip` or `it.todo` wrappers from the COLR-01 and COLR-02 test blocks so they run actively. All tests should now pass.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/color-contrast.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All text-corbusier-blue/red usages as text color replaced with text-accent-primary/text-accent-danger. COLR-01 and COLR-02 tests enabled and passing. Full test suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/color-contrast.test.ts` — all COLR-01, COLR-02, COLR-03, COLR-04 tests pass
- `npx vitest run` — full suite passes (no regressions)
- Grep: `grep -rn "text-ink/[0-9]" src/components/ src/screens/` returns only decorative exemptions (vs, or separators)
- Grep: `grep -rn "text-corbusier-blue\|text-corbusier-red" src/components/ src/screens/` returns no matches (all replaced with accent tokens)
</verification>

<success_criteria>
1. No informational text uses text-ink with opacity below /70 in any component
2. All brand color text uses accent tokens that auto-switch in dark mode
3. Decorative separators ("vs", "or") retain WCAG-exempt low-opacity styling
4. All four COLR test blocks pass in color-contrast.test.ts
5. Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-color-and-contrast-audit/07-02-SUMMARY.md`
</output>
