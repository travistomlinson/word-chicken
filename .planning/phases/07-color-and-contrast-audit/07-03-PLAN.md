---
phase: 07-color-and-contrast-audit
plan: 03
type: execute
wave: 1
depends_on: ["07-02"]
files_modified:
  - src/screens/LobbyScreen.tsx
  - src/__tests__/color-contrast.test.ts
autonomous: true
gap_closure: true
requirements: [COLR-01, COLR-02, COLR-03, COLR-04]

must_haves:
  truths:
    - "LobbyScreen Join button uses text-charcoal on bg-corbusier-yellow, not text-white"
    - "COLR-03 test covers LobbyScreen yellow background elements"
    - "All 4 COLR requirements remain fully passing after the fix"
  artifacts:
    - path: "src/screens/LobbyScreen.tsx"
      provides: "Join button with WCAG AA compliant text on yellow background"
      contains: "text-charcoal"
    - path: "src/__tests__/color-contrast.test.ts"
      provides: "COLR-03 test coverage for LobbyScreen"
      contains: "LobbyScreen"
  key_links:
    - from: "src/screens/LobbyScreen.tsx"
      to: "src/index.css"
      via: "text-charcoal references --color-charcoal token"
      pattern: "text-charcoal"
    - from: "src/__tests__/color-contrast.test.ts"
      to: "src/screens/LobbyScreen.tsx"
      via: "readFileSync reads LobbyScreen source for COLR-03 pattern check"
      pattern: "LobbyScreen"
---

<objective>
Fix the one COLR-03 gap found during verification: LobbyScreen Join button uses text-white on bg-corbusier-yellow (2.03:1 contrast — fails WCAG AA). Extend test coverage to prevent regression.

Purpose: Close the single verification gap so Phase 7 achieves 8/8 must-haves and all COLR requirements are fully satisfied.
Output: Fixed LobbyScreen.tsx and expanded color-contrast.test.ts
</objective>

<execution_context>
@C:/Users/travi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/travi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-color-and-contrast-audit/07-02-SUMMARY.md

<interfaces>
<!-- From src/screens/LobbyScreen.tsx line 190 (the violation): -->
```tsx
className="bg-corbusier-yellow text-white font-jost font-bold uppercase px-6 py-3 rounded-lg shadow-md hover:shadow-lg hover:scale-[1.02] active:scale-[0.98] transition-all duration-200 cursor-pointer disabled:opacity-40 disabled:cursor-not-allowed"
```

<!-- From src/__tests__/color-contrast.test.ts COLR-03 describe block (lines 8-55): -->
<!-- Tests read TileCard and ConfigScreen but NOT LobbyScreen -->
```typescript
describe('COLR-03: Yellow background contrast', () => {
  const tileCardSource = readFileSync(resolve(__dirname, '../components/TileCard.tsx'), 'utf-8')
  const configSource = readFileSync(resolve(__dirname, '../screens/ConfigScreen.tsx'), 'utf-8')
  // ... tests for TileCard and ConfigScreen only
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Join button and extend COLR-03 test coverage</name>
  <files>src/screens/LobbyScreen.tsx, src/__tests__/color-contrast.test.ts</files>
  <action>
1. In `src/screens/LobbyScreen.tsx` line 190, change `text-white` to `text-charcoal` in the Join button className string. The full className becomes:
   `"bg-corbusier-yellow text-charcoal font-jost font-bold uppercase px-6 py-3 rounded-lg shadow-md hover:shadow-lg hover:scale-[1.02] active:scale-[0.98] transition-all duration-200 cursor-pointer disabled:opacity-40 disabled:cursor-not-allowed"`
   Do NOT change anything else in the file.

2. In `src/__tests__/color-contrast.test.ts`, inside the existing `COLR-03: Yellow background contrast` describe block (after the ConfigScreen tests around line 54), add a new test:

```typescript
it('LobbyScreen Join button does not use text-white on yellow bg', () => {
  const lobbySource = readFileSync(resolve(__dirname, '../screens/LobbyScreen.tsx'), 'utf-8')
  const lines = lobbySource.split('\n')
  const violations = lines.filter(
    (line) =>
      line.includes('bg-corbusier-yellow') &&
      line.includes('text-white') &&
      !line.includes('?')
  )
  expect(violations).toEqual([])
})
```

This follows the exact same pattern used for the ConfigScreen Play a Friend button test (line-level check, ternary exclusion with `?` guard). Read the `lobbySource` at the top of the describe block alongside the existing `tileCardSource` and `configSource` reads.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/color-contrast.test.ts</automated>
  </verify>
  <done>LobbyScreen Join button uses text-charcoal on yellow background (5.61:1 contrast ratio). COLR-03 test suite includes LobbyScreen coverage. All color-contrast tests pass (expect 29+ tests passing, 0 failing).</done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite has no regressions</name>
  <files></files>
  <action>
Run the full Vitest suite to confirm no regressions from the LobbyScreen change. No file changes expected — this is a verification-only task.

Expected: All 176+ tests pass, 0 failures.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Full test suite passes with zero failures. No regressions introduced.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/__tests__/color-contrast.test.ts` — all COLR-03 tests pass including new LobbyScreen test
2. `npx vitest run` — full suite green, no regressions
3. Grep confirmation: no `bg-corbusier-yellow.*text-white` patterns remain in any .tsx file (excluding ternary conditionals)
</verification>

<success_criteria>
- LobbyScreen Join button className contains `text-charcoal` (not `text-white`)
- color-contrast.test.ts has a LobbyScreen COLR-03 test
- All color-contrast tests pass (29+ tests, 0 failures)
- Full test suite passes (176+ tests, 0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/07-color-and-contrast-audit/07-03-SUMMARY.md`
</output>
