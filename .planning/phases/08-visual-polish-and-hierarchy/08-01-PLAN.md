---
phase: 08-visual-polish-and-hierarchy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/visual-polish.test.ts
  - src/components/TurnIndicator.tsx
  - src/components/ChickenOMeter.tsx
autonomous: true
requirements:
  - PLSH-01
  - PLSH-03
  - PLSH-05

must_haves:
  truths:
    - "Turn indicator text is visually dominant over the round counter and Quit button in the top bar"
    - "ChickenOMeter bar is wide enough to read fill level at a glance on mobile"
    - "RoundEndCard overlay structure matches GameOverScreen exactly"
  artifacts:
    - path: "src/__tests__/visual-polish.test.ts"
      provides: "Static analysis tests for all PLSH requirements"
      contains: "PLSH-01"
    - path: "src/components/TurnIndicator.tsx"
      provides: "Visually dominant turn state indicator"
      contains: "text-lg"
    - path: "src/components/ChickenOMeter.tsx"
      provides: "Wider tension bar readable on mobile"
      contains: "w-8"
  key_links:
    - from: "src/__tests__/visual-polish.test.ts"
      to: "src/components/TurnIndicator.tsx"
      via: "readFileSync static analysis"
      pattern: "text-lg.*font-bold"
    - from: "src/__tests__/visual-polish.test.ts"
      to: "src/components/ChickenOMeter.tsx"
      via: "readFileSync static analysis"
      pattern: "w-8"
---

<objective>
Create the Phase 8 test scaffold and implement the three simplest visual polish fixes: TurnIndicator dominance (PLSH-01), ChickenOMeter width (PLSH-03), and RoundEndCard overlay verification (PLSH-05).

Purpose: Establish the test infrastructure for all five PLSH requirements and ship the straightforward CSS-level fixes that don't involve API changes.
Output: visual-polish.test.ts with all five test stubs (three passing, two failing for Plan 02), plus updated TurnIndicator and ChickenOMeter components.
</objective>

<execution_context>
@C:/Users/travi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/travi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-polish-and-hierarchy/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/TurnIndicator.tsx:
```typescript
interface TurnIndicatorProps {
  phase: TurnPhase
  currentPlayerId: string
}
// baseClass = 'font-jost uppercase tracking-wider text-sm py-2 text-center transition-all duration-300'
// FIX: text-sm (14px) must become text-lg (18px) for visual dominance over text-[10px] round counter
```

From src/components/ChickenOMeter.tsx:
```typescript
interface ChickenOMeterProps {
  wordLength: number
}
// Current: 'relative h-48 sm:h-64 w-5 rounded-full overflow-hidden gradient-tension'
// FIX: w-5 (20px) must become w-8 (32px)
```

From src/__tests__/color-contrast.test.ts (test pattern to follow):
```typescript
import { readFileSync } from 'fs'
import { resolve, basename } from 'path'
const source = readFileSync(resolve(__dirname, '../components/ComponentName.tsx'), 'utf-8')
expect(source).toContain('w-8')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create visual-polish.test.ts with all PLSH test stubs</name>
  <files>src/__tests__/visual-polish.test.ts</files>
  <action>
Create `src/__tests__/visual-polish.test.ts` following the exact Phase 7 test pattern from `src/__tests__/color-contrast.test.ts` (readFileSync + static source analysis).

Include five describe blocks:

1. **PLSH-01: TurnIndicator visual dominance** — Read TurnIndicator.tsx source. Assert `baseClass` string contains `text-lg` (not `text-sm`). Assert it contains `font-bold`.

2. **PLSH-02: ScorePanel round score hierarchy** — Read ScorePanel.tsx source. Assert it does NOT contain `_roundScores` (the underscore-prefixed unused alias). Assert it contains `roundScores` used in JSX (not just in the destructuring). Use `describe.skip` so this test appears in output but is marked for Plan 02.

3. **PLSH-03: ChickenOMeter tension bar width** — Read ChickenOMeter.tsx source. Assert the bar container class contains `w-8` (not `w-5`).

4. **PLSH-04: Staged tile distinct visual state** — Read PlayerHand.tsx source. Assert that staged tiles do NOT use `isStaged ? 'concrete'` pattern (meaning staged tiles must use a different color than 'concrete'). Also assert staged tiles are NOT `disabled={isStaged}` (they must remain clickable for unstaging). Use `describe.skip` so this test appears in output but is marked for Plan 02.

5. **PLSH-05: RoundEndCard overlay consistency** — Read both RoundEndCard.tsx and GameOverScreen.tsx source. Assert RoundEndCard contains `fixed inset-0` and `bg-black/50` and `animate-fade-in`. Assert inner card contains `bg-card max-w-md` and `rounded-2xl` and `animate-scale-in`. This test should pass immediately with no code changes (PLSH-05 is already met).

Use `basename()` for Windows path compatibility in any error messages (matching Phase 7 pattern). Import `resolve` and `basename` from `path`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/visual-polish.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with 5 describe blocks. PLSH-05 passes. PLSH-02 and PLSH-04 are skipped. PLSH-01 and PLSH-03 fail (red — components not yet updated).</done>
</task>

<task type="auto">
  <name>Task 2: Fix TurnIndicator dominance and ChickenOMeter width</name>
  <files>src/components/TurnIndicator.tsx, src/components/ChickenOMeter.tsx</files>
  <action>
**TurnIndicator.tsx (PLSH-01):**
In the `baseClass` string on line 13, change `text-sm` to `text-lg`. The `font-bold` is already applied conditionally on individual return branches — this is fine. The size change from 14px to 18px creates an unambiguous dominance gap over the `text-[10px]` round counter (10px) and `text-xs` Quit button (12px).

Do NOT change anything else in TurnIndicator. The conditional `font-bold` on active states and the `animate-pulse` dot are correct as-is.

**ChickenOMeter.tsx (PLSH-03):**
On line 18, in the className array, change `w-5` to `w-8`. This widens the tension bar from 20px to 32px. The center `flex-1` column in GameScreen absorbs this 12px delta — this is safe per research analysis (Pitfall 3).

Do NOT change the height classes (`h-48 sm:h-64`) or the `rounded-full` or any other classes. Only the width class changes.

After both edits, run the full test suite to confirm no regressions. The PLSH-01 and PLSH-03 tests should now pass. Existing color-contrast tests must remain green.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/visual-polish.test.ts && npx vitest run src/__tests__/color-contrast.test.ts</automated>
  </verify>
  <done>PLSH-01 test passes (TurnIndicator has text-lg). PLSH-03 test passes (ChickenOMeter has w-8). PLSH-05 test passes (already met). Full test suite green. No regressions in color-contrast tests.</done>
</task>

</tasks>

<verification>
npx vitest run src/__tests__/visual-polish.test.ts — 3 tests pass (PLSH-01, PLSH-03, PLSH-05), 2 skipped (PLSH-02, PLSH-04)
npx vitest run — full suite green, no regressions
</verification>

<success_criteria>
- TurnIndicator baseClass uses text-lg (18px), creating clear size dominance over text-[10px] round counter and text-xs Quit button
- ChickenOMeter bar is w-8 (32px), readable as a tension indicator on mobile
- RoundEndCard overlay confirmed matching GameOverScreen (no changes needed)
- visual-polish.test.ts exists with all 5 requirement test stubs
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-polish-and-hierarchy/08-01-SUMMARY.md`
</output>
